# Commit message format issues

Four problematic commit messages demonstrating common issues:
1. Missing conventional commit type (should be `fix: ...`)
2. Vague description that doesn't explain what was fixed
3. WIP commit that should be squashed
4. Multiple unrelated changes in one commit
