# Missing documentation

All three public items (`process_data`, `Config`, `connect`) lack doc comments. Public API items should have documentation explaining purpose, parameters, return values, and any panics/errors.
